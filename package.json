{
  "name": "verifychain",
  "version": "1.0.0",
  "private": true,
  "description": "VerifyChain dynamic full-stack product authenticity platform",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test:smoke": "node tests/smoke.mjs",
    "test:e2e": "playwright test",
    "test": "npm run test:smoke && npm run test:e2e"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "helmet": "^8.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  }
}
