services:
  - type: web
    name: verifychain
    runtime: static
    buildCommand: npm ci && npm run test:smoke
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
