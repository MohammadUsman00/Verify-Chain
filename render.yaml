services:
  - type: web
    name: verifychain
    runtime: node
    buildCommand: npm ci && npm run test:smoke
    startCommand: npm start
    envVars:
      - key: VC_DEMO_MODE
        value: "true"
